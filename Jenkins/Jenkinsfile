node {
  def app

  stage('Clone Repo') {
    checkout scm
  }

  stage('Build app image') {
    sh 'cd app'
    sh 'ls -la'
    app = docker.build('app:jenkins')
  }

  stage('Clean up') {
    sh 'rm -rf *'
  }
}