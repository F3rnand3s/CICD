node {
  def app
  def nginx

  stage('Clone Repo') {
    checkout scm
  }

  stage('Build app image') {
    app = docker.build('app:jenkins', 'app/')
  }

  stage('Build nginx image') {
    app = docker.build('nginx:jenkins', 'nginx/')
  }

  stage('Clean up') {
    sh 'rm -rf *'
  }
}