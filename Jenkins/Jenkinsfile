pipeline {
    agent {
        docker { label 'my_node_label' image 'node:7-alpine' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}